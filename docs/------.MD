## Tarea 9: Mejorar manejo de estado global

### Responsable: 1 desarrollador

### Tiempo estimado: 2-3 días

### Prioridad: 🟡 Media

### Problema actual

- No hay estado global definido
- Prop drilling en algunos componentes
- Falta de context para autenticación
- No se usa Zustand o Redux

### Solución propuesta

#### Archivos a crear:

1. `src/store/authStore.ts` - Store de autenticación
2. `src/store/uiStore.ts` - Store de UI (modales, sidebar)
3. `src/contexts/AuthContext.tsx` - Context de autenticación

#### Implementación (opción con Zustand)

```typescript
// src/store/authStore.ts
import { create } from "zustand";
import { persist } from "zustand/middleware";
import { AuthUser } from "@/types/user";

interface AuthState {
	user: AuthUser | null;
	isAuthenticated: boolean;
	login: (user: AuthUser) => void;
	logout: () => void;
}

export const useAuthStore = create<AuthState>()(
	persist(
		(set) => ({
			user: null,
			isAuthenticated: false,
			login: (user) => set({ user, isAuthenticated: true }),
			logout: () => set({ user: null, isAuthenticated: false }),
		}),
		{
			name: "auth-storage",
		}
	)
);
```

### Checklist

- [ ] INSTALAR Zustand
- [ ] Crear stores store de authenticacion


---
